import './assets/main.css' // Estilo Global do Vue.js. Caso um CSS seja aplicado para todas as páginas, deve ser importado aqui

import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')
